# Stage 1: Development
FROM node:20-alpine

WORKDIR /app

# Cache dependencies
COPY package*.json ./
RUN npm ci

# Copy source
COPY . .

# Vite development port
EXPOSE 5173

# Start development server with host binding
# (Host binding is also configured in vite.config.ts)
CMD ["npm", "run", "dev", "--", "--host"]
